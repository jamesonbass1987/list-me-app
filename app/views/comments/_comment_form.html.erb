<%= form_for([commentable, commentable.comment.build]) do |f| %>
  <%= form_errors_check(comment) %>
  <%= f.hidden_field :location_id, :value => listing.location_id %>
  <%= f.label :content, "Comment" %>
  <%= render 'comment_content', locals: {f:f} %>
<% end %>
