<%= render partial: 'comment', locals: {comment: @comment.commentable} %>
<%= render partial: 'comments/comment_form', locals: {comment: @comment, statuses: nil} %>
<%= render 'listing_return_button' %>
