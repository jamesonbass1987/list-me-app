<%= form_for [listing.location, listing, commentable] do |f| %>
  <%= form_errors_check(commentable) %>

  <%= content_tag :div, :class => 'form-group' do %>
    <%= f.label :comment_status_id, "Status" %>
    <%= f.collection_select(:comment_status_id, statuses, :id, :name, {}, {:class => 'form-control custom-select btn'}) %>
  <% end %>

  <%= render partial: 'comments/comment_content', locals: {f:f} %>

<% end %>
