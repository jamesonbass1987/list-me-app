<%= form_for(user, url: login_path, layout: :basic) do |f| %>
  <%= form_errors_check(user) %>

  <%= f.label :username %>
  <%= f.text_field :username%><br>
  <%= f.label :password %>
  <%= f.password_field :password%><br>

  <%= f.submit 'Sign In'%>

<% end %>
