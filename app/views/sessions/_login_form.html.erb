<%= form_for(user, url: '/login') do |f| %>
  <%= form_errors_check(user) %>
  <%= render partial: 'users/username_password_fields', locals: {f: f} %>
  <%= f.submit 'Sign In', :class => 'btn btn-outline-success btn-block'%>
<% end %>
