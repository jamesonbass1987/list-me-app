<%= content_tag :div, :class => "#{image_carousel_class_tag(listing_image.listing, listing_image)}" do %>
    <%= image_tag listing_image.image_url, :class => "#{carousel_image_class_tag(listing_image)}"%>
<% end %>
