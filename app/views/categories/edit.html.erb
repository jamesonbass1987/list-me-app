<%= tag.h1("Edit Category", :class => 'heading-text') %>

<%= render partial: 'form', locals: {category: @category} %>
<%= link_to "Delete Category", category_path(@category), :method => :delete, :class => 'btn btn-block btn-outline-danger', data: { confirm: 'Are you sure you want to delete this category?' } %>
